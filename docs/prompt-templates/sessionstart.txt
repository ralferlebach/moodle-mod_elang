## SESSION-START: Sprachübung – mod_elang 2.0

Du arbeitest als erfahrener Moodle-Architekt und PHP-Entwickler
an einem laufenden Projekt. Lies zunächst alles, was ich dir gebe,
bevor du antwortest.

---

### A) Lastenheft, Pflichtenheft und Blueprint

Die aktuellen Dokumente liegen im Plugin-Repo:
  docs/materials/Lastenheft_Pflichtenheft_Blueprint.md   (verbindlich)
  docs/materials/Blueprint_kompakt.md                    (Arbeitsfassung)
  docs/materials/Machbarkeit_Zusatzanforderungen.md      (Exporte, Icon, YouTube, KI)
  docs/materials/Migration_V1_V2.md                      (Migration und Restore)
  docs/materials/Lizenz_und_Herkunft.md                  (CeCILL-B/GPL, Namensfrage)
  docs/materials/Ideen_Backlog.md                        (bewertetes Backlog)

---

### B) Kontext-Dokument der letzten Sitzung

Das aktuelle Kontext-Dokument (sessionende der Vorsitzung) ist im Anhang
zu finden. Ältere Kontext-Dokumente zwecks Referenz: docs/sessions/

---

### C) Aktueller Code

Repository: https://github.com/ralferlebach/moodle-mod_elang/tree/development
Der aktuelle Plugin-Stand liegt zusätzlich als Patch-ZIP im Anhang, einzuspielen
in die Moodle-Installation unter mod/elang/.

Moodle: >= 4.5 LTS (2024100700) bis 5.3 LTS. supported = [405, 503].
        Releaseziel 5.3 (Code Freeze 24.08.2026, Release 05.10.2026).
        KEINE Abwärtskompatibilität < 4.5.
PHP:    8.1 bis 8.4 — Code strikt auf PHP-8.1-Sprachstand beschränken!
        (4.5 -> 8.1-8.3, 5.0 -> 8.2-8.3, 5.2/5.3 -> 8.3-8.4)
DB:     PostgreSQL und MariaDB/MySQL.

Haupt-Dependencies:
  KEINE. Optionale Integrationen (KI-Subsystem, OAuth 2, Dateikonverter, ffmpeg)
  werden zur Laufzeit erkannt und degradieren sauber. Nicht in version.php
  deklariert.

Vorgänger: mod_elang 1.x (Université de La Rochelle, CeCILL-B, Moodle 3.4).
  Version 2.0 ist eine kompatible NEUENTWICKLUNG, kein Refactoring.
  Aus V1 wird KEIN Code übernommen (siehe Lizenz_und_Herkunft.md).

---

### D) Aktueller Entwicklungsstand

**Version:** 2.0.0-alpha.5 (2026072304) — Infrastruktur + versioniertes
Datenmodell + zwei-Algorithmen-Bewertung + Domänenschicht + vollständige
Schreib-API + Privacy-Provider + vollständige Lese-API (6 External Functions
insgesamt) + Transkript-Maskierung. Player, Autorenwerkzeug, Hilfestufen,
Completion/Gradebook, Reporting und Exporte fehlen noch vollständig.

**Grundsätze (nicht ohne explizite Diskussion ändern):**
  A — Kompatible Neuentwicklung: fachlich kompatibel (Formate, Bewertungsregeln,
      Migration), technisch neu. Keine Portierung von V1-Code.
  B — Ausschließlich dokumentierte Moodle-APIs. Keine eigenen Endpunkte, kein
      eigenes HTML-Dokument, kein Fremd-Frontend-Framework.
  C — Kein Release ohne vollständige Privacy-API, ohne CSRF-/Capability-
      Absicherung aller schreibenden Operationen, ohne verlustfreie
      Inhaltsbearbeitung.

**Zuletzt abgeschlossen (Session 005):**
  - classes/local/domain/transcript_masker.php: ersetzt jede Lücken-
    Zeichenspanne im Transkript durch ein {{gap:<gapkey>}}-Token.
    elang_cue.transcript enthält den VOLLEN Originaltext (Lösungswörter
    wortwörtlich drin, da charstart/charlength Positionen INNERHALB dieses
    Texts sind) -- JEDE Funktion, die ein Transkript zurückgibt, MUSS das
    zuerst durchlaufen. Offsets als Unicode-Codepoints (mb_substr), nicht
    Bytes; überlappende Lücken werfen coding_exception statt still ein
    ggf. noch lösungshaltiges Transkript zu erzeugen. Mit PHPUnit-Tests
    UND eigenständigem Smoke-Test (9/9, Codepoint- vs. Byte-Fall gegen
    echtes mb_strpos() verifiziert, nicht von Hand gezählt).
  - classes/external/get_exercise.php: statische Versionskennzahlen
    (elangid, versionid, language, totalcues, totalgaps, contenthash) --
    kein Inhalt, Player kann per contenthash Cache-Gültigkeit prüfen,
    bevor überhaupt etwas geholt wird.
  - classes/external/get_cues.php: paginierte Cues (offset/limit, Deckel
    200) mit Gaps, Transkript IMMER transcript_masker-maskiert. Gaps geben
    BEWUSST kein charstart/charlength zurück -- die Zeichenlänge der
    Lösung wäre ein kostenloser, unangeforderter "Wortlänge"-Hinweis,
    obwohl Hinweise laut elang_gaphint bewusst anfragbar/mit Abzug versehen
    sein sollen. Solution/elang_gapanswer werden für diese Funktion gar
    nicht erst abgefragt.
  - classes/external/get_attempt_state.php: Aggregate + je beantworteter
    Lücke resultstate/accepted/tries/hintlevel UND der eigene, vorher
    eingegebene Antworttext (für Wiederaufnahme -- eigener Text der
    lernenden Person, keine Lösung). Nutzt attempt_helper::
    require_attempt_ownership() aus alpha.4 mit.
  - db/services.php: alle drei als type=read, ajax=true, App-fähig
    registriert (mod/elang:view bzw. mod/elang:attempt).
  - Tests: tests/local/domain/transcript_masker_test.php,
    tests/external/{get_exercise_test,get_cues_test,
    get_attempt_state_test}.php (u. a. End-zu-Ende-Nachweis: Lösungswort
    erscheint NIE im zurückgegebenen Transkript, Gaps tragen keine
    Zeichenpositions-Felder).
  - version.php: 2026072303 -> 2026072304 (2.0.0-alpha.5) -- notwendig,
    damit db/services.php neu eingelesen wird.
  - php -l/phpcs (0/0) und phpcpd (No clones found) real gegen den
    gesamten Baum geprüft; Blueprint Kap. 6.1/7, Blueprint_kompakt.md,
    CHANGELOG aktualisiert (6 von aktuell geplanten External Functions
    jetzt implementiert: get_exercise, get_cues, get_attempt_state,
    start/submit/finish_attempt).
  - Noch NICHT gegen eine echte Moodle-Instanz gelaufen -- nach dem Muster
    der letzten Runden ist beim nächsten echten Testlauf mit mind. einem
    kleineren Fund zu rechnen.

Ältere Session-Details stehen vollständig in docs/sessions/session-00{2,3,4,5}.md.

**Als nächstes geplant (Phase 2 — Hilfestufen, dann Completion/Gradebook):**
  - Hilfeanfragen: mod_elang_request_hint + attempt_manager-Anbindung
    (elang_gaphint wird bisher nicht konsultiert; hintlevel/hintedgaps
    bleiben bis dahin auf 0). Muss dieselbe Eigentümerschaftsprüfung wie
    submit_response/finish_attempt nutzen (attempt_helper). Antworttext
    einer bereits mit Hilfe versehenen Lücke: Score-Abzug pro Hint-Level
    berücksichtigen (bisher: score = correctgaps/totalgaps, ohne Abzug)
  - Completion auf den Aggregaten (exactgaps/correctgaps/...), Gradebook-
    Anbindung (Domänenlogik liefert die Aggregate bereits, Anbindung fehlt)
  - elang.answermaxlength (aktivitätsweiter Default; pro-Lücke-Override
    existiert bereits als elang_gap.maxlength) — würde die aktuell harte
    500-Zeichen-Grenze in submit_response ablösen bzw. ergänzen
  - Migration V1 -> V2 als wiederaufnehmbarer Ad-hoc-Task — BLOCKIERT auf
    einen V1-Datensimulator, da keine echten V1-Bestandsdaten mehr verfügbar
    sind (siehe Migration_V1_V2.md, Kap. 1.1)
  - classes/courseformat/overview.php (wirkt ab Moodle 5.0, auf 4.5 inaktiv)

**Für 2.1 verbindlich zugesagt (Blueprint Kap. 19):**
  2.1-1 regelbasierte Lückenerzeugung
  2.1-2 Antwortvariante nachträglich anerkennen inkl. Neubewertung
  2.1-3 Zeichenleiste für Sonderzeichen
  2.1-4 Übersetzungsspur als Hilfestufe
  2.1-5 Blindmodus und Audio-only
  Die Vorleistungen dafür MÜSSEN in 2.0 ins Datenmodell: stabile cuekey/gapkey,
  dauerhaft gespeicherter Antworttext, zustandsfreie Bewertungsfunktion.

---

### E) Ziel dieser Sitzung

Wir knüpfen an die Arbeiten an, die im aktuellen Kontext-Dokument
und in der angehängten Code-Base vorliegen, und gehen, wenn der letzte
Arbeitsschritt als vollumfänglich erfüllt angesehen wird, zum nächsten
vorgesehenen Schritt in der Umsetzung über.

Schlüssel-Entwurfsentscheidungen (nicht verändern ohne explizite Diskussion):
  1. Versionierte Übungsdefinition. Inhaltsänderungen erzeugen eine neue Version;
     laufende Versuche bleiben an der alten Version. Kein Löschen von Lerndaten
     beim Speichern — das war P0.1 in V1.
  2. Keine eigene AJAX-Schicht. Alles über External Functions + core/ajax.
     Destruktives nur per bestätigtem POST bzw. Moodleform.
  3. Lösungen und Antwortvarianten verlassen den Server nicht. Prüfung
     ausschließlich serverseitig. Keine Lösungstexte in Events.
  4. Eine Zeile je fachlichem Objekt. Keine JSON-Blobs. Fortschritt und Abschluss
     über indizierte Aggregate auf elang_attempt.
  5. Ausgabe über Renderables, Renderer und Mustache; JavaScript als native
     ES-Module unter amd/src. Kein jQuery, kein Enyo, kein Bower.
  6. Reporting über Report Builder; Datenexport über die Dataformat API
     (CSV/XLSX/ODS/JSON ohne formatspezifischen Eigencode).
  7. Migration wird an der EXISTENZ der Legacy-Tabellen festgemacht, nicht an
     Versionsnummern. Legacy-Tabellen werden erst in einem späteren Release
     abgebaut.
  8. Keine harten Fremdabhängigkeiten; alles Optionale zur Laufzeit erkannt.
  9. Versionsabhängige APIs über Fähigkeitsprüfung (Klassen-/Funktionsexistenz),
     NIE über Versionsvergleiche im Fachcode. Betrifft vor allem:
     courseformat\overview (ab 5.0), core_ai-Aktionen (4.5 kennt nur
     generate_text und generate_image), Report-Builder-Erweiterungen.
  10. Bewertung: GENAU zwei benannte Algorithmen (exact, wordrecognized), keine
      frei kombinierbaren Einzelschalter. resultstate (feinste gefundene
      Klassifizierung) und accepted (Regelentscheidung für DIESE Lücke) bleiben
      getrennte Felder. Schriftsystemabhängige Reduktion läuft über
      script_handler_manager -> elangscript-Subplugin oder latin_script_handler
      als Default. Regex-Antwortvarianten sind ein eigener Mechanismus (zählen
      bei Treffer immer als exact), keine dritte Toleranzstufe.

Bekannte Stolperfallen (Coding-Standards und Testumgebung):
  - Lang-Dateien: alphabetisch sortiert, keine Kommentar-Trennzeilen
  - Keine Leerzeile nach öffnender { (Klasse, Methode, Block)
  - Genau 1 Space nach Komma; keine Alignment-Paddings
  - Inline-Kommentare: Großbuchstabe/Ziffer am Anfang, Satzzeichen am Ende
  - Keine MOODLE_INTERNAL-Guards in Klassen-Dateien (classes/); auch nicht in
    reinen Funktions-/Generator-Dateien ohne Seiteneffekte (lib.php, tests/generator)
  - Kein "TODO"/"@todo"-Token in ausgeliefertem Code (moodlecheck verlangt MDL-Ref)
  - @param-Typ muss dem tatsächlichen Type-Hint exakt entsprechen
  - @covers-Annotation in jeder Testklasse
  - Tabellen- und Feldnamen: durchgehend Kleinbuchstaben, Feldnamen ohne Underscore
  - Statische Register in Tests via setUp zurücksetzen (resetAfterTest setzt nur
    die DB zurück, keine Statics)
  - Moodle 5.3 ist noch nicht veröffentlicht: 5.3-CI-Jobs sind bewusst
    nicht blockierend
  - PHP 8.1 ist die Untergrenze: KEINE readonly-Klassen, KEINE DNF-Typen,
    KEINE typisierten Klassenkonstanten, KEINE Property Hooks
  - PHPUnit-Metadaten als @covers-Annotation, nicht als Attribut
    (Attribute stehen auf Moodle 4.5 nicht zur Verfügung)
  - Mehrzeilige if-Bedingungen: öffnende Klammer allein auf der if-Zeile, erster
    Ausdruck auf der Folgezeile, schließende Klammer allein auf eigener Zeile
    vor { (phpcbf korrigiert das automatisch, aber es lohnt sich, gleich richtig
    zu schreiben)
  - Wer ein Interface implementiert, braucht auf JEDER implementierenden Methode
    ein EIGENES Docblock — das Docblock im Interface reicht moodlecheck/phpcs
    nicht, auch wenn es inhaltlich redundant wirkt
  - Keine literalen Backticks in Strings (auch nicht als einzelnes Zeichen in
    einer Zuordnungstabelle) — \u{0060} in einem DOPPELT gequoteten String
    verwenden; \u{}-Escapes werden in einfach gequoteten Strings NICHT
    interpretiert
  - #[\Override]-Attribut ist PHP 8.3+, verstößt gegen die 8.1-Untergrenze
  - moodle-plugin-ci: --no-init + manuelles "cd moodle && php admin/tool/
    {phpunit,behat}/cli/init.php" ist ein veralteter v3-Workaround und in v4
    fragil/nicht mehr nötig; moodle-plugin-ci phpunit/behat initialisieren
    selbst (siehe aktuelles gha.dist.yml als Referenz)
  - GitHub Actions: continue-on-error auf JOB-Ebene (z. B.
    "continue-on-error: ${{ matrix.experimental }}" direkt unter dem Job)
    maskiert zwar den Job-RESULT für needs:-Gates, lässt aber den GESAMTEN
    Workflow-Lauf trotzdem als fehlgeschlagen erscheinen — sieht für alle
    Praktiker aus wie "CI ist kaputt", obwohl die eigentlich blockierenden
    Kombinationen liefen. Nicht-blockierende/experimentelle Matrix-Einträge
    (z. B. Moodle main/5.3-dev) deshalb IMMER in einen EIGENEN, dedizierten
    Job auslagern und dort continue-on-error: true auf JEDEM EINZELNEN Step
    setzen, nicht auf Job-Ebene. Der eigene Job wird dann NICHT in needs: der
    ci-complete-Gate aufgenommen.
  - Moodle main (5.3-dev, oder jeder unveröffentlichte Entwicklungszweig)
    unter der neuesten PHP-Version kann echte debugging()-Aufrufe beim
    frischen Install auslösen (moodle-plugin-ci prüft das über
    MoodleProcess::hasDebuggingMessages(), Regex auf "++ Text ++\n* line" —
    das reale Moodle-debugging()-Ausgabeformat, kein Fehlalarm des Tools).
    Das ist erwartete Instabilität eines unveröffentlichten Branches, kein
    Plugin-Fehler — gehört in einen experimentellen Job, nicht gefixt
  - Vor dem Ausliefern lokal prüfen, sofern ein PHP mit intl-Extension
    verfügbar ist: php -l auf jede Datei, phpcs --standard=moodle gegen den
    ganzen Baum (moodlehq/moodle-cs via Composer installierbar)
  - Subplugin-Verzeichnisse (db/subplugins.json) MÜSSEN physisch existieren,
    auch wenn leer — Git trackt keine leeren Verzeichnisse. Ohne Datei darin
    (z. B. README.md) loggt Moodle bei jedem Plugin-Scan "Invalid subtype
    directory '...' detected", auch ohne echten Fehlerfall
  - XMLDB: NOTNULL-CHAR-Spalten dürfen KEIN DEFAULT="" haben — Moodles
    XMLDB-Validator lehnt das mit einem debugging()-Aufruf ab ("must have
    one meaningful DEFAULT declared or none"). Entweder ein echter Default
    oder DEFAULT-Attribut ganz weglassen. Wenn weggelassen: Anwendungscode
    MUSS das Feld beim Insert immer explizit setzen (Moodle-DB-Layer wirft
    sonst wegen NOT NULL ohne Default)
  - Moodles $DB-Layer garantiert KEINE Integer-PHP-Typen für Integer-Spalten:
    insert_record() liefert echte int zurück, get_record()/get_field()
    liefern auf MariaDB/PDO oft Strings. assertSame() ist typstrikt — bei
    jedem Vergleich von frisch aus der DB geholten numerischen Werten
    IMMER explizit (int) casten, auf BEIDEN Seiten
  - PHPUnit-Testfixtures (Test-Doubles, die von mehreren *_test.php-Dateien
    gebraucht werden): NIEMALS in eine eigene, nicht auf _test.php endende
    Datei auslagern — Moodles PHPUnit-Discovery requiret NUR Dateien nach
    dem Muster *_test.php, alles andere wird nie geladen ("Class ... not
    found"). Korrekt (bestätigt gegen echten Moodle-Core-Test
    lib/phpunit/tests/advanced_test.php): Datei unter tests/fixtures/,
    Namespace passend zum Pfad (z. B. mod_elang\fixtures\...), UND
    explizites require_once(__DIR__ . '/pfad/zu/fixtures/datei.php') in
    setUpBeforeClass() jeder verbrauchenden Testklasse
  - phpcs (moodle-Standard): GENAU eine Klasse pro Datei, keine Ausnahme —
    auch nicht für kleine Test-Doubles, die neben der eigentlichen
    Testklasse in derselben Datei "praktisch" wären
  - \externallib_advanced_testcase (Basisklasse für External-Function-Tests)
    braucht wie fake_script_handler ein explizites require_once($CFG->
    dirroot . '/webservice/tests/helpers.php'), sonst "Class ... not found".
    BESSER, wenn keine ihrer webservice-spezifischen Zusatzmethoden gebraucht
    werden (nur execute()/execute_returns() direkt aufgerufen, keine echte
    REST-Dispatch-Simulation): einfach \advanced_testcase direkt erben. Die
    Legacy-Klasse ist ohnehin ab Moodle 4.6 zur Ablösung vorgesehen und
    empfiehlt isolierte Prozesse wegen interner class_alias()-Aufrufe —
    beides Gründe, sie zu meiden, wenn sie nicht wirklich gebraucht wird.
    Allgemeine Lektion: JEDE Moodle-Kernklasse mit "advanced_testcase" im
    Namen prüfen, ob sie zur Kernbibliothek gehört (immer verfügbar) oder zu
    einem Subsystem mit eigenem tests/helpers.php (braucht require_once).
  - phpcpd (Teil von moodle-plugin-ci phpcpd, läuft mit || true, blockiert
    also nie die CI) trotzdem ernst nehmen, wenn gemeldet — fast identische
    setUp()-Methoden mehrerer Testklassen gehören in eine gemeinsame Fixture
    (tests/fixtures/), nicht kopiert. WICHTIG dabei, real durchlaufen (nicht
    nur einmal, sondern es kam beim ersten Versuch selbst zu einem Fehler):
    eine gemeinsame Fixture als TRAIT ("use TraitName;" INNERHALB der
    Klasse) wird beim KOMPILIEREN der Klasse aufgelöst — also sobald die
    Datei geladen wird, VOR jeder Methode, auch vor einer statischen
    setUpBeforeClass(). require_once() in setUpBeforeClass() kommt für einen
    Trait deshalb IMMER zu spät ("Trait ... not found"), obwohl exakt
    dasselbe Muster für fake_script_handler (eine normale Klasse, nur per
    new innerhalb von Methodenrümpfen referenziert) funktioniert. Ein
    require_once() auf Dateiebene VOR der Klasse würde den Trait zwar
    rechtzeitig laden, verstößt dann aber gegen eine ANDERE echte phpcs-
    Regel (moodle-Standard: ausführender Code auf Dateiebene außerhalb eines
    MOODLE_INTERNAL-Kontexts gilt als unerwünschte globale Zustandsänderung).
    RICHTIG: geteilte Fixtures als normale KLASSE mit statischer
    create()-Factory-Methode bauen, nur aus Methodenrümpfen (setUp()) heraus
    aufgerufen — dann funktioniert das bewährte require_once()-in-
    setUpBeforeClass()-Muster unverändert, keine Dateiebenen-Anweisung nötig.
  - Vor dem Ausliefern eines require_once()/Autoloading-Fixes: nicht nur
    php -l und phpcs laufen lassen (die finden diese Art von "zu spät
    aufgelöst"-Reihenfolgefehler NICHT), sondern ein eigenes Skript bauen,
    das GENAU die Aufrufreihenfolge von PHPUnit nachstellt (Datei laden ->
    Klasse deklariert -> setUpBeforeClass() aufrufen -> erst DANACH auf die
    Fixture zugreifen) und prüfen, ob die Klasse zu jedem Zeitpunkt im
    erwarteten Zustand (geladen/nicht geladen) ist
  - PHPUnit assertContains() vergleicht seit PHPUnit 9 STRIKT (===), vorher
    (PHPUnit 8 und früher) lose. Dieses Projekt läuft auf PHPUnit 9.6 (siehe
    Moodle-4.5-Testlauf). Betrifft also GENAU dieselbe String-vs-Int-Falle
    wie bei DB-Rückgabewerten (siehe oben) — nur diesmal in der Assertion
    selbst, nicht im geprüften Code. Bei jedem assertContains()/
    assertNotContains() auf einer aus der DB stammenden ID-Liste: beide
    Seiten explizit (int) bzw. array_map('intval', ...) casten.
    assertEqualsCanonicalizing()/assertEquals() sind davon NICHT betroffen
    (lose Vergleichslogik, unverändert), nur assertContains()/
    assertNotContains() sind strikt geworden.
  - Ein fehlendes Verzeichnis ist nicht automatisch ein Fehler, der behoben
    werden muss — manchmal ist "existiert noch nicht" der korrekte,
    erwartete Zustand für die aktuelle Projektphase (Beispiel: templates/
    für Mustache, bevor der Renderer/Ausgabe-Layer überhaupt gebaut ist).
    Unterschied zum script/-Fall (Session 003): DORT verlangte MOODLE SELBST
    die Existenz einer im Schema deklarierten Subplugin-Verzeichnis-Struktur
    unabhängig vom Inhalt — HIER ist es nur unser eigenes Dev-Tool, das ein
    Verzeichnis für einen noch nicht gebauten Projektteil erwartet. Bei
    eigenen Tools: die Meldung an den tatsächlichen Erwartungszustand
    anpassen (OK statt ERROR), statt das Verzeichnis vorzeitig anzulegen.
  - External Functions: self::validate_context($context) ruft bei
    CONTEXT_MODULE INTERN require_login($course, false, $cm, ...) auf —
    DAS prüft bereits, ob die Aktivität für die aktuelle Person "sichtbar"
    ist (u. a. über mod/MODNAME:view, die Capability, die Moodle per
    Namenskonvention für Sichtbarkeit heranzieht). Wird GENAU DIESE
    Capability (":view") prohibited, schlägt bereits validate_context()
    mit \core\exception\require_login_exception ("Activity is hidden") fehl
    — der EIGENE, nachfolgende require_capability(':view', ...)-Aufruf wird
    nie erreicht. Tests, die ":view" prohibiten und
    required_capability_exception erwarten, schlagen deshalb fehl (real
    bestätigt gegen Moodle 4.5). RICHTIG: bei ":view"-Tests
    require_login_exception erwarten. Bei ANDEREN Capabilities (z. B.
    ":attempt", die nicht sichtbarkeitsrelevant sind) funktioniert die
    ursprüngliche Erwartung (required_capability_exception) weiterhin normal,
    da dort kein require_login()-Kurzschluss greift.

---

### F) Modus zur Delivery

- Code- UND Doku-Änderungen immer gemeinsam ausliefern als EIN Patch-ZIP:
  eine ZIP-Datei, die NUR die geänderten Dateien enthält (kein .git-Ordner),
  mit korrekter Verzeichnisstruktur relativ zum Plugin-Root — also auch
  geänderte docs/-Dateien MIT im selben ZIP, nicht separat oder als lose
  Einzeldateien im Chat.
- Patch-ZIPs erhalten einen durchnummerierten Namen (patch-2.0.YY.zip),
  die Version in version.php orientiert sich am aktuellen Datum und wird
  mit jeder Iteration hochgezählt (Format: YYYYMMDDNN, z.B. 2026072301)
- Zu jeder Änderung eine einzeilige Kurzbeschreibung des Commits (englisch)
  sowie bis zu 5 Stichpunkte zu den vorgenommenen Änderungen.
  Diese Angaben als MD-Code DIREKT UNTER dem Download-Link der Patch-ZIP
- Bei der Code-Generierung immer die Moodle-Coding-Standards einhalten:
    - GPL-Boilerplate-Header in jeder Datei
    - PHPDoc für alle Klassen, Methoden und Properties
    - Short array syntax ([] statt array())
    - Keine Leerzeile nach öffnender {
    - Multiline-Argumente: jedes Argument auf eigener Zeile
    - Genau 1 Space nach Komma in Argumentlisten (kein Alignment-Padding)
    - Inline-Kommentare: Großbuchstabe/Ziffer am Anfang, Punkt/!/? am Ende
    - Keine MOODLE_INTERNAL-Guards in Klassen-Dateien (classes/)
    - Membervariablen: all lowercase (kein camelCase)
    - Lang-Strings: alphabetisch sortiert, keine Kommentar-Trennzeilen
    - PHPUnit-Tests: basic_testcase ohne DB, advanced_testcase mit DB
    - @covers-Annotation in jeder Testklasse
- Author: Ralf Erlebach, Jahr: 2026, Lizenz: GPL v3 or later
- Bei Unklarheiten und auftretenden Fehlern: Shell-Skripte, SQL-Abfragen
  und JS-Skripte für die Konsole mitliefern. Diagnose-Skripte werden
  grundsätzlich nur im Chat-Fenster ausgeliefert, nie als Download!

Bevor du beginnst:
1. Bestätige kurz, was du aus dem Kontext-Dokument und den
   Dateien verstanden hast.
2. Benenne, falls vorhanden, Widersprüche oder fehlende
   Informationen, die du für die heutige Aufgabe brauchst.
3. Warte dann auf meine Freigabe, bevor du Code schreibst.

Liefere nun eine kurze Einschätzung zu den bevorstehenden Arbeiten
und den drei Punkten oben!
